# Touch Targets Audit Configuration

# Directory to scan (relative to project root)
SCAN_DIR=.

# Comma-separated file extensions to scan
FILE_TYPES=svelte,vue,jsx,tsx,html,css,scss,less

# Minimum touch target size in pixels (WCAG 2.5.8 Minimum = 24px, Enhanced = 44px)
MIN_TARGET_PX=44
