# Unused CSS Audit Configuration

# Build command that emits CSS warnings (run from BUILD_DIR)
BUILD_CMD=npm run build

# Directory to run the build from (relative to project root)
BUILD_DIR=.

# Grep pattern to match unused CSS warnings in build output
PATTERN=Unused CSS selector
